import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.communityroster.app',
  appName: 'communityrostercom.communityroster.app',
  webDir: 'dist'
};

export default config;
