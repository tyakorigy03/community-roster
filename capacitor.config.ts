import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.blessingcommunity.app',
  appName: 'Blessing Community Roster',
  webDir: 'dist'
};

export default config;